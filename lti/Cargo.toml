[package]
name = "lti"
version = "0.3.0-alpha.2"
edition = "2024"

[features]
axum = [
  "dep:lti-launch"
]

[dependencies]
lti-launch = { path = "../lti-launch", optional = true }
lti-spec = { path = "../lti-spec"}
