[package]
name = "lti"
version = "0.3.0"
edition = "2024"

[features]
axum = [
  "dep:lti-claims",
  "dep:lti-launch"
]

[dependencies]
lti-claims = { path = "../lti-claims", optional = true }
lti-launch = { path = "../lti-launch", optional = true }
lti-spec = { path = "../lti-spec"}
